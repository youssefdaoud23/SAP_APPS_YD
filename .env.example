# Invarture App Studio - local/Docker defaults
PORT=8081

# Optional HTTP Basic protection.
# Leave both blank for local-only testing. Set both before exposing the service.
APP_STUDIO_USER=
APP_STUDIO_PASSWORD=

# Persistent workspace inside the Docker volume.
WORKSPACE_FILE=/app/data/workspace.json

# Optional: allow HTTP SAP endpoints outside localhost for lab environments only.
# Keep false in production.
SAP_ALLOW_HTTP=false

# Start with no SAP connections. Add real connections only after the UI is working.
SAP_CONNECTIONS_JSON=[]

# Example SAP Basic connection:
# SAP_CONNECTIONS_JSON=[{"id":"sap-dev","name":"SAP DEV","type":"OData V2","baseUrl":"https://sap-dev.example.com/sap/opu/odata/sap/ZMY_SERVICE_SRV/","auth":"basic","usernameEnv":"SAP_DEV_USER","passwordEnv":"SAP_DEV_PASSWORD","sapClient":"100","sapLanguage":"EN"}]
# SAP_DEV_USER=replace_me
# SAP_DEV_PASSWORD=replace_me

# Bearer example:
# SAP_CONNECTIONS_JSON=[{"id":"sap-api","name":"SAP API","type":"OData V4","baseUrl":"https://example.com/sap/opu/odata4/.../","auth":"bearer","tokenEnv":"SAP_API_TOKEN"}]
# SAP_API_TOKEN=replace_me

# OAuth2 client-credentials example:
# SAP_CONNECTIONS_JSON=[{"id":"sap-btp","name":"SAP BTP","type":"REST","baseUrl":"https://api.example.com/","auth":"oauth2-client-credentials","tokenUrl":"https://auth.example.com/oauth/token","clientIdEnv":"SAP_CLIENT_ID","clientSecretEnv":"SAP_CLIENT_SECRET","scope":"my.scope"}]
# SAP_CLIENT_ID=replace_me
# SAP_CLIENT_SECRET=replace_me
